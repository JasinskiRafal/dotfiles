interface:
  display_name: "Create Plan"
  short_description: "Turn an idea into a reviewed plan"
  default_prompt: "Use $create-plan to turn this idea into a reviewed implementation plan."
policy:
  allow_implicit_invocation: false
